import { IChatPreference } from "./chat.interface";

// eslint-disable-next-line @typescript-eslint/no-unused-vars
const createChatPreference = async (payload: IChatPreference) => {};

export const ChatPreferenceService = {
  createChatPreference,
};
