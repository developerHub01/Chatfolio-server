import { IMessagePreference } from "./message.preference.interface";

// eslint-disable-next-line @typescript-eslint/no-unused-vars
const createMessagePreference = async (payload: IMessagePreference) => {};

export const MessagePreferenceService = {
  createMessagePreference,
};
