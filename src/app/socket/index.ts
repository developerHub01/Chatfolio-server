import { Server, Socket } from "socket.io";


// eslint-disable-next-line @typescript-eslint/no-unused-vars
export const registerSocketHandler = (socket: Socket, io: Server) => {
  // registerDummySocketHandlers(socket, io);
};
